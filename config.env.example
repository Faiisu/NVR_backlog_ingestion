# Copy to config.env and fill in real values. config.env is gitignored.
# Default credentials: NVR_USER=admin, default password candidates tried: "admin", "Mc158806"
NVR_USER=admin
NVR_PASSWORD=admin
